@org.hibernate.annotations.FilterDef(
        name = "countryFilter",
        parameters = {
                @org.hibernate.annotations.ParamDef(name = "country", type = String.class)
        }
)

package dev.project.hibernateFilterImplementation.entity;

